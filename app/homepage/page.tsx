"use client"
import Homepage from '../../components/homepage/Homepage';

export default function HomepagePage({ tutorialState }: { tutorialState: any }) {
    return <Homepage tutorialState={tutorialState} />;
}